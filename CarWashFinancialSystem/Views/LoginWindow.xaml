<Window x:Class="CarWashFinancialSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CarWash Pro - Вход" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundDarkBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Logo/Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
            <TextBlock Text="🚗" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="CarWash Pro" Style="{StaticResource ModernHeaderText}" HorizontalAlignment="Center"/>
            <TextBlock Text="Финaнсовая система" Style="{StaticResource ModernBodyText}" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Login Form -->
        <Border Grid.Row="2" Style="{StaticResource ModernCardStyle}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Логин" Style="{StaticResource ModernBodyText}" Margin="0,0,0,8"/>
                <TextBox x:Name="UsernameTextBox" 
                         Background="#3A3A3A" 
                         Foreground="{StaticResource TextPrimaryBrush}"
                         BorderBrush="#404040" 
                         BorderThickness="1"
                         Padding="12"
                         FontSize="14"
                         Text="admin"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource ModernCardStyle}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Пароль" Style="{StaticResource ModernBodyText}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordBox"
                             Background="#3A3A3A"
                             BorderBrush="#404040"
                             BorderThickness="1"
                             Padding="12"
                             FontSize="14"
                             Password="admin"/>
            </StackPanel>
        </Border>

        <!-- Login Button -->
        <Button Grid.Row="4" Content="Войти в систему" 
                Style="{StaticResource ModernActionButtonStyle}"
                Height="45"
                FontSize="15"
                Click="LoginButton_Click"/>

        <!-- Footer -->
        <TextBlock Grid.Row="6" Text="v1.0 • CarWash Financial System" 
                   Style="{StaticResource ModernBodyText}"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom"
                   Opacity="0.6"/>
    </Grid>
</Window>